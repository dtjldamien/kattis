#include <iostream>
#include <string>

using namespace std;

int main() {
  string x0;
  cin >> x0;
  while (x0 != "END") {
    int number = stoi(x0);
    int num_digits = 1;
    // wip
    cout << num_digits << endl;
    cin >> x0;
  }
}